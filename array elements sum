//sum of array no
#include<stdio.h>
int main()
{
	int a[100],i,n,sum=0;
	printf("enter limit:");
	scanf("%d",&n);
	printf("\n enter n no:");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		sum=a[i]+sum;
	}
	printf("\n sum=%d",sum);
}
